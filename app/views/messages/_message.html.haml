.contents__right__mid__user1
  = message.user.name
.contents__right__mid__date1
  = message.created_at.strftime("%Y/%m/%d %H:%M")
- if message.content.present?
  %p.contents__right__mid__chat1
    = message.content
= image_tag message.image.url, class: '.contents__right__mid__chat1__image' if message.image.present?